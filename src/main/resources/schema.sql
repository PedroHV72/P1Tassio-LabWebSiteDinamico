CREATE TABLE categoria(
      id                  INT            NOT NULL,
      ImagemSimboloUrl    VARCHAR (1000),
      nome                VARCHAR (40)   NOT NULL,
      descricao           VARCHAR (300)
);